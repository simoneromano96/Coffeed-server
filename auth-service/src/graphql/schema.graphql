schema {
  query: Query
  mutation: Mutation
}

type User {
  id: ID! @juniper(ownership: "owned")
  email: String!
  username: String!
  password: String!
  userType: UserType!
}

type UserType {
  id: String!
  name: String!
  grants: [String!]!
}
